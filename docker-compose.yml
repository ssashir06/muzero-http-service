version: "3.7"

services:
  muzero-http:
    build: .
    volumes:
      - ./models/_for_test/tictactoe:/app/models/tictactoe
    environment:
      - MODEL_PATH_tictactoe=models/tictactoe/model.checkpoint
      - CUDA_VISIBLE_DEVICES=
    ports:
      - "8000:8000"